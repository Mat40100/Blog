{%extends 'admin_layout.twig' %}

{% block navbar %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top" id="sideNav">
        <a class="navbar-brand js-scroll-trigger" href="#page-top">
            <span class="d-block d-lg-none">Administration</span>
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link js-scroll-trigger" href="?p=home">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link js-scroll-trigger" href="?p=blog">Blog</a>
                </li>
                {% if session.Userlvl is defined %}
                    <li class="nav-item">
                        <a class="nav-link js-scroll-trigger" onclick="changement();" href="?p=home&d=disconnect">Se deconnecter</a>
                    </li>  
                {% endif %}
                {% if session.Userlvl is defined %}
                    <p class="font-light font-weight-light text-light bg-secondary rounded">{{ session.Nickname }}</p>
                {% endif %}
            </ul>
        </div>
    </nav>
{% endblock %}